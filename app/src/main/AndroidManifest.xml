<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Abude"
        tools:targetApi="31">
        <activity
            android:name=".Stok_Pengeluaran"
            android:exported="false" />
        <activity
            android:name=".Stok"
            android:exported="false" />
        <activity
            android:name=".Login"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Beranda_Owner" />
        <activity android:name=".Beranda_Purchasing" />
        <activity android:name=".Beranda_Admin" />
        <activity android:name=".Pembelian" />
        <activity android:name=".Pengeluaran" />
    </application>

</manifest>